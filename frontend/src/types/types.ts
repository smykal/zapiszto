export type BodyParamsItem = {
                              dict_body_params_name: string;
                              value: number;
                              insert_date: string;
  };

export type BmiItem = {
                        value: number;
                        date: string;
                        description: string;
}

  